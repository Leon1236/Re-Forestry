# ARB-11.9f — Creative tabs, item models, lang polish

**Date:** 2026-07-25  
**Status:** Implemented

## What landed

- Creative tab now lists `LEAVES_DEFAULT` + `LEAVES_DEFAULT_FRUIT` (decorative/pods/boats/charcoal were already present)
- Pod `models/item` + MC 26.2 `items/` defs (`pods_cocoa/dates/papaya/coconut`)
- Charcoal family `items/` defs (`charcoal`, `log_pile`, `decorative_log_pile`, `ash_block`) — `models/item` already existed
- Lang: `item.reforestry.sapling` / `pollen_fertile` fallbacks; `block.reforestry.sapling_ge`; readable “Date Pod”
- Regenerator: `python3 tools/generate_arb_11_9f_assets.py`

## Skipped

- JEI plugin — JEI is not a Gradle dependency (CE `ArboricultureJeiPlugin` would need optional JEI first)
- Boat models were already generated by `tools/generate_boat_assets.py` (43+43)

## Note

Genetic `leaves` / `sapling_ge` blocks have no BlockItem (`null` factory); germlings use `item.reforestry.sapling` / `pollen_fertile`.
